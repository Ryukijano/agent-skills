---
description: Cloud GPU & SSH workflow
---

# Cloud GPU & SSH Workflow

Skill: `.devin/skills/cloud-gpu-ssh/SKILL.md`

## Steps
1. Read the skill at `.devin/skills/cloud-gpu-ssh/SKILL.md`
2. Identify which of the 11 tools is needed
3. Run the tool in CLI mode or invoke via MCP
4. Verify output

## CLI Quick Test
```bash
python3 mcp_servers/cloud_gpu_ssh/server.py --cli list_machines
```
