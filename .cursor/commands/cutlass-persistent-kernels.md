# cutlass-persistent-kernels

Quick reference for `cutlass persistent kernels`.

- Architecture / framework: ask first.
- Key command / env vars: see the full skill.
- Verification: run the smallest failing test first.
