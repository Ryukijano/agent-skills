# Review Security

Run a readonly Security Review subagent on local changes.

Apply skill: `review-security`.

Launch one `security-review` subagent with `readonly: true`, `Diff: branch changes`.

Report vulnerabilities, unsafe patterns, and credential leaks.
