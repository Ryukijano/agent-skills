# Setup ML Project

Scaffold a new ML research project with standard directory structure.

Creates `core_app/`, `configs/`, `scripts/`, `jobs/`, `tests/`, `.cursor/skills/`, requirements, SLURM template.

Full procedure: `.windsurf/workflows/setup-ml-project.md`
