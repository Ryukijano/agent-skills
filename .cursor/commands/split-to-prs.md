# Split to PRs

Split current work into small, reviewable pull requests.

Apply skill: `split-to-prs`.

1. Inventory changed files and logical groupings.
2. Propose split plan (user approves before execution).
3. Create branches per group; cherry-pick or restage commits.
4. Open separate PRs with focused descriptions and test plans.
