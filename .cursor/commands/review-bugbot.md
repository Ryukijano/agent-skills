# Review Bugbot

Run a readonly Bugbot subagent review on local branch changes.

Apply skill: `review-bugbot`.

Launch one `bugbot` subagent with `readonly: true`, `Diff: branch changes` (or `uncommitted changes` if requested).

Report findings as: critical bugs, improvements, suggestions.
