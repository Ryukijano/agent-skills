# MOT HOTA Eval

Apply `mot-training-workflow` skill (eval section).

```bash
cd /home/aimsgroupuol/AIMSgeneral/Gyanateet_tracking
conda activate surgi_track
```

Run smoke-stratified HOTA on Stage 3 vs Stage 4 lean checkpoints. Tune `birth_score` / `min_hits` if zero predictions.

See `workflows/devin/mot-hota-eval.devin.md` for defaults.
